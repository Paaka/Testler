import React from 'react';
import MainTemplate from '../../templates/MainTemplate';

const SetView = () => {
    return (
    <MainTemplate>
        <p>Hello this is set view</p>
    </MainTemplate>
    );
};

export default SetView;