import React from 'react';
import MainTemplate from '../templates/MainTemplate';

const NewSetView = () => {
    return (
    <MainTemplate>
        <p>NewSetView</p>
    </MainTemplate>
    );
};

export default NewSetView;